import XCTest

// Placeholder so the target compiles before real snapshot tests are added.
final class PlaceholderSnapshotTest: XCTestCase {
    func testTargetCompiles() {
        XCTAssertTrue(true)
    }
}
